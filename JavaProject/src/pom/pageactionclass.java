package pom;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.FindBy;

public class pageactionclass {

	/*
	 * { WebDriver driver; PageObjects Loginpo; // Create Instance to Page Factory
	 * class publicPageActions_Login(WebDriver driver); { this.driver = driver; //
	 * set webDriver for current Page Action } }
	 * 
	 * @FindBy(xpath=".//*[@id=’Email’]") public WebElementgmailUserIDWebEdit() {
	 * 
	 * }
	 */
	
}
